- Global instance
_dumper = TensorDumper()

# Public API
init = _dumper.init
dump = _dumper.dump
compare = _dumper.compare
enable = _dumper.enable
disable = _dumper.disable
update_metadata = _dumper.update_metadata这块是如何进行使用的
- Global instance
_dumper = TensorDumper()

# Public API
init = _dumper.init
dump = _dumper.dump
compare = _dumper.compare
enable = _dumper.enable
disable = _dumper.disable
update_metadata = _dumper.update_metadata这块是如何使用的